spring:
  application:
    name: processor
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: inventory-processor-group
      auto-offset-reset: earliest
    streams:
      application-id: inventory-streams-app
      properties:
        default.key.serde: org.apache.kafka.common.serialization.Serdes$StringSerde
        # Value serde is set explicitly per Consumed/Produced in KafkaStreamsConfig (safer than a no-arg JsonSerde).
        default.value.serde: org.apache.kafka.common.serialization.Serdes$ByteArraySerde
